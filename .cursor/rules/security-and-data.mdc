---
alwaysApply: true
---

---
description: 企业级 Next.js + TS 项目安全、数据校验、数据库、缓存、队列规则
globs:
  - "**/*.{ts,tsx}"
alwaysApply: true
---

你生成的所有数据处理、接口、表单、数据库、缓存、队列代码，都必须默认遵循“安全优先、强校验、类型安全、幂等优先”的原则。

# 一、安全默认原则

默认认为所有输入都不可信，包括：
- 表单输入
- URL 参数
- Query 参数
- Route Handler 请求体
- Server Action 参数
- 外部服务响应
- 环境变量
- 队列消息体

默认必须做到：
1. 最小权限
2. 不信任输入
3. 不暴露敏感信息
4. 优先校验再执行
5. 尽早失败，可控降级

# 二、Zod 校验规则

所有输入输出必须进行 Schema 校验，统一优先使用 Zod。

必须校验的对象包括：
- 表单入参
- params
- searchParams
- request body
- server action 入参
- 环境变量
- 第三方接口返回结构
- 队列消息

要求：
- 先定义 schema
- 再基于 schema 推导类型
- 校验失败返回明确错误
- 不允许跳过服务端校验只做前端校验

推荐模式：
- `schema.parse()` 用于必须严格失败的场景
- `schema.safeParse()` 用于需要自定义错误响应的场景

# 三、接口与返回结构规范

生成 Route Handler / Server Action / 服务端 use case 时，必须包含：
- 入参校验
- 权限校验
- 返回值类型
- 错误处理
- 写操作幂等说明
- 边界场景处理

推荐统一返回结构：

```ts
export type ApiResponse<T> =
  | { success: true; data: T }
  | { success: false; error: { code: string; message: string } };