/**
 * Build a deterministic welcome message for demo pages.
 * @param projectName The project display name.
 * @returns Welcome message text.
 */
export function createWelcomeMessage(projectName: string): string {
  return `Welcome to ${projectName}. This structure is ready for feature-based development.`;
}
